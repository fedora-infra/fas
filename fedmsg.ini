[fedmsg]
# This is the endpoint from which fas will broadcast messages.
publish_endpoint = tcp://*:6543

# This is the environment we're running in.  It only modifies the topic
# name.  It can be one of 'dev', 'stg', or 'prod'.
environment = dev
